{
  "name": "@statrookie/root",
  "version": "0.1.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "npx lerna run build",
    "build:core": "npx lerna run build --scope @statrookie/core",
    "build:sharks": "npx lerna run build --scope @statrookie/sharks",
    "start": "npx lerna run start",
    "start:core": "npx lerna run start --scope @statrookie/core",
    "start:sharks": "npx lerna run start --scope @statrookie/sharks",
    "dev:sharks": "npx concurrently \"yarn run start:core\" \"npx lerna run dev --scope @statrookie/sharks\""
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "lerna": "^6.4.1",
    "typescript": "^4.4.3"
  }
}
